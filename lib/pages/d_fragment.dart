import 'package:flutter/material.dart';

class DFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("D Fragment",style: TextStyle(color: Colors.black),)),
    );
  }
}