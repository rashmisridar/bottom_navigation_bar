import 'package:flutter/material.dart';

class CFragment extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("C Fragment",style: TextStyle(color: Colors.black),)),
    );
  }
}
